generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ExampleEntity {
  id        String   @id
  name      String   
  email     String   @unique
  birthDate DateTime
}